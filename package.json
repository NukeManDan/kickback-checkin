{
  "name": "Kickback-Check-In",
  "version": "1.0.0",
  "description": "Kickback Auto Check-In Web Interface",
  "productName": "Kickback Check-In",
  "cordovaId": "org.cordova.quasar.app",
  "capacitorId": "",
  "author": "NukeManDan <nukemandan@protonmail.com>",
  "private": true,
  "scripts": {
    "dev": "quasar dev",
    "build": "quasar build",
    "test": "echo \"No test specified\" && exit 0",
    "deploy": "push-dir --dir=dist/spa --remote=gh-pages --branch=master"
  },
  "dependencies": {
    "@quasar/extras": "^1.0.0",
    "quasar": "^1.0.0"
  },
  "devDependencies": {
    "@quasar/app": "^1.0.0"
  },
  "engines": {
    "node": ">= 8.9.0",
    "npm": ">= 5.6.0",
    "yarn": ">= 1.6.0"
  },
  "browserslist": [
    "last 1 version, not dead, ie >= 11"
  ]
}
